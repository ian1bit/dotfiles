const age = 19

if (age > 18) {
  console.log('Voce tem mais de 18 anos')
}

const simpsions = ['Marge', 'Homer', 'Lisa', 'Bart']

if (simpsions.length >= 3) {
  console.log('O array tem bastante personagens')
}
